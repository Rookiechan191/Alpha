# Environment
.env

# Corpus, index, and generated data (rebuild via make ingest)
data/raw/
data/index/
data/chunks.jsonl

# Eval outputs (per-question results are large, regenerate with make eval)
eval/results.json
# metrics.json is committed so reviewers can see numbers without running eval

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.egg

# Sentence-transformers model cache
.cache/
